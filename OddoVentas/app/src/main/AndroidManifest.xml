<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rexv666480.oddoventas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_odoo72"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_odoo72"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IniciarSesionActivity"
            android:theme="@style/FullScreen">

        </activity>
        <activity
            android:name=".ClientesActivity"
            android:label="Clientes"
            android:theme="@style/AppTheme" />

        <activity android:name=".ProductosActivity" />
        <activity android:name=".PedidoVentasActivity"  />

        <activity android:name=".AgregarPedidoVentaActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".SplashActivity"   android:theme="@style/FullScreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>


    </application>

</manifest>